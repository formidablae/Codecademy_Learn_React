describe('', function () {
    it('', function () {
        expect(() => {
            require('../containers/GuineaPigsContainer.js');
        }, 'Make sure that **GuineaPigsContainer.js** is in a folder named `containers`, and ensure that folder is neighbors with the `components` folder.').to.not.throw();
    });
});
